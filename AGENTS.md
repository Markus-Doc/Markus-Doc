# Agent instructions

This is the compiled public release for markuswalker.com. Cloudflare Pages publishes `main` (the static resume site) from the repository root, and `dev` (the interactive site in progress) as a branch preview. The editable source is the separate local `portfolio-website` repository. See `DEPLOYMENT.md`.

- Keep runtime files and public downloads byte for byte unless Markus has approved a site change.
- Keep `README.md` as the GitHub profile. Put release instructions in `DEPLOYMENT.md`.
- Check links, routing and licences before changing files in the release root.
- Show Markus proposed public copy, resume changes and generated artwork before implementing or publishing them.
- Do not push or deploy without Markus's explicit authorisation.
